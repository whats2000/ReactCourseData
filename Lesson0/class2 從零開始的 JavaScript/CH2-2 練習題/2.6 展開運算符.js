// 合併以下兩個陣列
const arr1 = [1, 2, 3];
const arr2 = [4, 5, 6];

// You code here
const mergeArr = [________, _______];
// End of your code

console.log(mergeArr);

/*
 * 等效於以下程式碼，不過解構賦值的方式不止可以用在陣列，也可以用在物件
 * console.log(arr1.concat(arr2));
 */

// 展開 User A 物件，並修正 age 的值
const userA = {
    name: 'John',
    age: 180,
}

// You code here
const fixedUserA = {________, age: 18};
// End of your code

console.log(fixedUserA);