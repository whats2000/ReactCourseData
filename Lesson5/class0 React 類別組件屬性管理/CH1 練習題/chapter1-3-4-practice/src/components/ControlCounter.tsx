/**
 * 創建一個子組件 `ControlCounter`，從父組件接收 `changeCount` 與 `resetCount` 方法。
 *  - 子組件中包含三個按鈕，分別對應增加 1、減少 1、重置 count 狀態。
 *  - 點擊按鈕時，調用父組件的 `changeCount` 或 `resetCount` 方法，實現 count 狀態的增減和重置。
 */
import { Component } from 'react'

interface IControlCounterProps {
  // You code here


  // End of your code
}

class ControlCounter extends Component<IControlCounterProps> {
  render() {
    // You code here

    // End of your code

    return (
      <>
        {/* You code here */}

        {/* End of your code */}
      </>
    );
  }
}

export default ControlCounter;
